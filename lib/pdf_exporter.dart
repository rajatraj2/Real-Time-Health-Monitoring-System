import 'package:flutter/material.dart';

class PDFExporter extends StatelessWidget {
  const PDFExporter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Export to PDF'),
      ),
      body: const Center(
        child: Text('PDF Export functionality coming soon!'),
      ),
    );
  }
}
